/** Set before navigating to `/designer/results` after a successful generate; consumed once on results mount. */
export const RESULTS_ENTRY_WELCOME_KEY = 'realizeme:resultsEntryWelcome';

/** Server `history.id` from session generate (used to persist CLIP search rows). */
export const HISTORY_ID_STORAGE_KEY = 'realizeme:historyId';

/** tldraw store snapshot JSON to load on /designer (from history “Continue”). */
export const RECOVER_SKETCH_STORAGE_KEY = 'realizeme:recoverSketch';
